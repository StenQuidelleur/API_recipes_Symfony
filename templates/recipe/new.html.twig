{% extends 'base.html.twig' %}

{% block title %}New Recipe{% endblock %}

{% block body %}
    <h1>Create new Recipe</h1>
    <div class="col-6">
    {{ include('recipe/_form.html.twig') }}
    <a href="{{ path('recipe_index') }}" class="btn btn-warning mt-2">back to list</a>
    </div>
{% endblock %}
