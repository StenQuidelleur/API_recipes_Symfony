{{ form_start(form) }}
    {{ form_widget(form) }}
    <a href="{{ path('ingredient_new') }}" class="btn btn-primary">Add new ingredient</a>
    <button class="btn btn-success">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}
